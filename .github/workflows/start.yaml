name: Test

on:
  pull_request:
    types:
      - opened
      - reopened
      - labeled
      - synchronize

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Init..."
      - uses: actions/checkout@v3
      - name: XD
        uses: ./.github/actions/test/action.yaml
        with:
          status: Success