name: Test

on:
  pull_request:
    types:
      - opened
      - reopened
      - labeled
      - synchronize

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello world!"
      - name: XD
        uses: ./.github/actions/action-a.yaml
        with:
          status: Success