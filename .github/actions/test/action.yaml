name: Test workflow
inputs:
  status:
    description: 'The status to announce'
    required: true
    type: string

runs:
  using: composite
  steps:
    - run: echo "Status is ${{ inputs.status}}"
      
