name: Test workflow
inputs:
  status:
    description: 'The status to announce'
    required: true
    type: string
    default: 'unknown'

runs:
  using: composite
  env:
      STATUS: 'failed xd'
  steps:
    - name: Status
      shell: bash
      run: echo "Status is ${{ env.STATUS}}"
      
