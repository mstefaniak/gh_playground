name: Test workflow 3
inputs:
  status:
    description: 'The status to announce'
    required: true
    default: 'unknown'

runs:
  using: composite
  steps:
    - name: Notify
      shell: bash
      run: node ./.github/actions/test/test.js
      
