name: Test workflow
inputs:
  status:
    description: 'The status to announce'
    required: true
    type: string
    default: 'unknown'

runs:
  using: composite
  env:
      STATUS: 'test123'
  steps:
    - name: Status
      shell: bash
      run: echo "The status is ${{ env.STATUS}}"
      
