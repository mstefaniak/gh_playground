name: Test workflow 3
inputs:
  status:
    description: 'The status to announce'
    required: true
    type: string
    default: 'unknown'

runs:
  using: composite
  steps:
    - name: Notify
      shell: bash
      run: ./.github/actions/test/notify.sh
      
